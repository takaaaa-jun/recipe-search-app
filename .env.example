# ================================
# PostgreSQL コンテナの設定
# Docker が PostgreSQL コンテナを起動するときに読み込む変数
# ================================

# データベース名
POSTGRES_DB=your_database_name
# データベースのユーザー名
POSTGRES_USER=your_database_user
# データベースのパスワード
POSTGRES_PASSWORD=your_database_password

# ================================
# アプリケーションの DB 接続設定
# バックエンドアプリ（Python/Django 等）が PostgreSQL に接続するときに読み込む変数
# ================================

# DB ホスト名（docker-compose のサービス名）
DB_HOST=db
# DB ポート番号（PostgreSQL のデフォルト: 5432）
DB_PORT=5432
# 接続先データベース名（POSTGRES_DB と合わせる）
DB_NAME=your_database_name
# 接続ユーザー名（POSTGRES_USER と合わせる）
DB_USER=your_database_user
# 接続パスワード（POSTGRES_PASSWORD と合わせる）
DB_PASSWORD=your_database_password